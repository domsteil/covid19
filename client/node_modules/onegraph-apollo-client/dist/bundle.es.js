function __$$styleInject(css, ref) {
  if ( ref === void 0 ) ref = {};
  var insertAt = ref.insertAt;

  if (!css || typeof document === 'undefined') { return; }

  var head = document.head || document.getElementsByTagName('head')[0];
  var style = document.createElement('style');
  style.type = 'text/css';

  if (insertAt === 'top') {
    if (head.firstChild) {
      head.insertBefore(style, head.firstChild);
    } else {
      head.appendChild(style);
    }
  } else {
    head.appendChild(style);
  }

  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
}

import ApolloClient from 'apollo-boost';

var classCallCheck = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};









var _extends = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];

    for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }

  return target;
};



var inherits = function (subClass, superClass) {
  if (typeof superClass !== "function" && superClass !== null) {
    throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
  }

  subClass.prototype = Object.create(superClass && superClass.prototype, {
    constructor: {
      value: subClass,
      enumerable: false,
      writable: true,
      configurable: true
    }
  });
  if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
};











var possibleConstructorReturn = function (self, call) {
  if (!self) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }

  return call && (typeof call === "object" || typeof call === "function") ? call : self;
};

function validateConfig(config) {
  if (!config.oneGraphAuth) {
    throw new Error('createApolloClient was called with invalid config: missing oneGraphAuth');
  }
}

var OneGraphApolloClient = function (_ApolloClient) {
  inherits(OneGraphApolloClient, _ApolloClient);

  function OneGraphApolloClient(config) {
    classCallCheck(this, OneGraphApolloClient);

    validateConfig(config);
    var oneGraphAuth = config.oneGraphAuth;

    var uri = new URL(oneGraphAuth.oneGraphOrigin);
    uri.pathname = '/dynamic';
    uri.searchParams.append('app_id', oneGraphAuth.appId);

    return possibleConstructorReturn(this, (OneGraphApolloClient.__proto__ || Object.getPrototypeOf(OneGraphApolloClient)).call(this, _extends({
      uri: uri.toString(),
      request: function request(operation) {
        return operation.setContext({ headers: oneGraphAuth.authHeaders() });
      }
    }, config)));
  }

  return OneGraphApolloClient;
}(ApolloClient);

export default OneGraphApolloClient;
export { OneGraphApolloClient };
//# sourceMappingURL=bundle.es.js.map
